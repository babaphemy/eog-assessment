[project]
name = "eog-assessment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "pathlib>=1.0.1", 
    "pydantic>=2.0.0",
]

[dependency-groups]
dev = [
     "pytest>=8.4.1",
    "pytest-asyncio>=1.0.0",
]
